1. Create a class called "Team"
2. Define private instance variables to hold the team's mascot name and the team's
   current score
3. Define a public constructor that takes one parameter and uses its value to
   initialize the mascot name, while initializing the score to 0
4. Define public "getters" for the two instance variables
5. Define a public method "score" that adds two points to the team's score
6. Define a main method (in that class or a driver class) that does the following
    a. Creates two teams (you pick the names)
    b. Has the first team score
    c. Has the second team score twice
    d. Uses an if-statement to print out the mascot name of whichever team has more points
